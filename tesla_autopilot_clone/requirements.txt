# Tesla Autopilot Clone — Python dependencies
# Install via:  pip install -r requirements.txt

ultralytics>=8.0.0      # YOLOv8 (includes PyTorch requirement)
opencv-python>=4.8.0    # Image & video I/O + rendering
numpy>=1.24.0           # Numerical operations
Pillow>=10.0.0          # Image utilities (used by ultralytics)

# Optional — for Jupyter notebook exploration
notebook>=7.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional — GPU acceleration (CUDA 11.8 example; adjust to your CUDA version)
# torch==2.0.1+cu118
# torchvision==0.15.2+cu118
